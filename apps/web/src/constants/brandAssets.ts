/**
 * Canonical branding URLs (served from /public).
 * - App icon: PWA / favicon family — header & marketing nav product mark.
 * - Sommi agent: character art — chat FAB & agent UI (matches Agent page avatar).
 */

export const APP_ICON_URL = '/icon-192.png';

/** Cache-bust when replacing the asset file. */
export const SOMMI_AGENT_ICON_URL = '/assets/sommelier-icon.png?v=5';
